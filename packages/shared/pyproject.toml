[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "axiom-shared"
version = "0.1.0"
description = "Shared types and utilities for Axiom"

[tool.setuptools]
packages = ["axiom_shared"]
